<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Blog Post</title>
    <!-- <link rel="stylesheet" href="/css/dashboard.css"> Ensure you adjust the path to your CSS -->
</head>

<body>

    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
            <a class="navbar-brand" href="#">TechBlog</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/customer/dashboard">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/auth/logout">Logout</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="edit-post-container">
        <h1>Edit Blog Post</h1>
        {{#if messages.success}}
        <div class="alert alert-success">{{messages.success}}</div>
        {{/if}}
        {{#if messages.error}}
        <div class="alert alert-danger">{{messages.error}}</div>
        {{/if}}
        <form action="/customer/editBlogPost/{{post.id}}" method="POST">
            <input type="hidden" name="_method" value="PUT"> <!-- Use method-override to allow PUT request -->
            <div class="form-group">
                <label for="title">Title:</label>
                <input type="text" id="title" name="title" value="{{post.title}}" required>
            </div>
            <div class="form-group">
                <label for="content">Content:</label>
                <textarea id="content" name="content" required>{{post.content}}</textarea>
            </div>
            <!-- Categories editing can be more complex depending on how categories are implemented -->
            <div class="form-group">
                <label for="categories">Categories:</label>
                <!-- Implement categories input here, possibly as a series of checkboxes -->
            </div>
            <button type="submit" class="btn">Update Post</button>
        </form>
        <form action="/customer/deleteBlogPost/{{post.id}}" method="POST"
            onsubmit="return confirm('Are you sure you want to delete this post?');">
            <input type="hidden" name="_method" value="DELETE"> <!-- Use method-override to allow DELETE request -->
            <button type="submit" class="btn btn-danger">Delete Post</button>
        </form>
    </div>
</body>

</html>